name: Build And Push Images To ACR

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  ACR_LOGIN_SERVER: ${{ secrets.ACR_LOGIN_SERVER }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ACR_LOGIN_SERVER }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-backend:latest
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-backend:${{ github.sha }}

      - name: Build and push degree service
        uses: docker/build-push-action@v6
        with:
          context: ./degree-recommendation-service
          file: ./degree-recommendation-service/Dockerfile
          push: true
          tags: |
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-degree-service:latest
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-degree-service:${{ github.sha }}

      - name: Build and push budget service
        uses: docker/build-push-action@v6
        with:
          context: ./budget_optimizer_service
          file: ./budget_optimizer_service/Dockerfile
          push: true
          tags: |
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-budget-service:latest
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-budget-service:${{ github.sha }}

      - name: Build and push career service
        uses: docker/build-push-action@v6
        with:
          context: ./career-service
          file: ./career-service/Dockerfile
          push: true
          tags: |
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-career-service:latest
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-career-service:${{ github.sha }}

      - name: Build and push scholarship service
        uses: docker/build-push-action@v6
        with:
          context: ./scholarship_and_loan_recommendation_service
          file: ./scholarship_and_loan_recommendation_service/Dockerfile
          push: true
          tags: |
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-scholarship-service:latest
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-scholarship-service:${{ github.sha }}

      - name: Resolve expected API URLs for frontend build
        shell: bash
        run: |
          # Frontend is built with deterministic FQDN format: <app>.<defaultDomain>
          DEFAULT_DOMAIN="${{ secrets.CONTAINERAPPS_DEFAULT_DOMAIN }}"
          echo "REACT_APP_BACKEND_URL=https://unifinder-backend.${DEFAULT_DOMAIN}" >> $GITHUB_ENV
          echo "REACT_APP_DEGREE_SERVICE_URL=https://unifinder-degree-service.${DEFAULT_DOMAIN}" >> $GITHUB_ENV
          echo "REACT_APP_BUDGET_SERVICE_URL=https://unifinder-budget-service.${DEFAULT_DOMAIN}" >> $GITHUB_ENV
          echo "REACT_APP_CAREER_SERVICE_URL=https://unifinder-career-service.${DEFAULT_DOMAIN}" >> $GITHUB_ENV
          echo "REACT_APP_SCHOLARSHIP_SERVICE_URL=https://unifinder-scholarship-service.${DEFAULT_DOMAIN}" >> $GITHUB_ENV

      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          build-args: |
            REACT_APP_BACKEND_URL=${{ env.REACT_APP_BACKEND_URL }}
            REACT_APP_DEGREE_SERVICE_URL=${{ env.REACT_APP_DEGREE_SERVICE_URL }}
            REACT_APP_BUDGET_SERVICE_URL=${{ env.REACT_APP_BUDGET_SERVICE_URL }}
            REACT_APP_CAREER_SERVICE_URL=${{ env.REACT_APP_CAREER_SERVICE_URL }}
            REACT_APP_SCHOLARSHIP_SERVICE_URL=${{ env.REACT_APP_SCHOLARSHIP_SERVICE_URL }}
          tags: |
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-frontend:latest
            ${{ env.ACR_LOGIN_SERVER }}/unifinder-frontend:${{ github.sha }}

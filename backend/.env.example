MONGO=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
JWT_SECRET=your-super-secret-jwt-key
# Comma-separated list of allowed frontend origins (no spaces unless part of URL)
CORS_ORIGINS=http://localhost:3000
# Scholarship service base URL (local Docker, Hugging Face Space, etc.)
SCHOLARSHIP_SERVICE_URL=http://localhost:5005
